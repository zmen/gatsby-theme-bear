{
  "name": "gatsby-theme-bear-workspace",
  "private": true,
  "license": "MIT",
  "scripts": {
    "develop": "yarn workspace example develop",
    "build": "yarn workspace example build",
    "build:prefix": "yarn workspace example build:prefix",
    "clean": "yarn workspace example clean",
    "serve": "yarn workspace example serve",
    "serve:prefix": "yarn workspace example serve:prefix"
  },
  "workspaces": [
    "gatsby-theme-bear",
    "example"
  ],
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/zmen/gatsby-theme-bear.git"
  }
}
